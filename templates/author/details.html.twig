<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de l'Auteur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Un header simple -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Bibliothèque</a>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card text-center">
                    <div class="card-header bg-warning text-white">
                        <h1>Détails de l'Auteur</h1>
                    </div>
                    <div class="card-body">
                        {# <img src="{{ asset(author.picture) }}" alt="{{ author.username }}" class="img-fluid rounded-circle mb-3" style="width: 150px;"> #}
                        <h2 class="card-title">{{ author.username | upper }}</h2>
                        <p class="card-text"><strong>Email :</strong> {{ author.email }}</p>
                        {# <p class="card-text"><strong>Nombre de livres :</strong> {{ author.nb_books }}</p> #}
                        <a href="{{ path('app_authorList') }}" class="btn btn-primary">Retour à la liste des auteurs</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

   

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
